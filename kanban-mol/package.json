{
    "name": "kanban-mol",
    "version": "1.0.0",
    "description": "Kanban board built with $mol framework",
    "scripts": {
        "start": "docker compose up --build",
        "stop": "docker compose down"
    }
}
