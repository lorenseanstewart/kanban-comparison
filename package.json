{
  "name": "kanban-comparison",
  "version": "1.0.0",
  "description": "Framework comparison project: Same app built in 10 frameworks",
  "private": true,
  "scripts": {
    "measure:single": "tsx scripts/measure-single.ts",
    "measure:final": "tsx scripts/measure-final.ts --runs 10",
    "measure:all": "npm run measure:final && npm run generate:charts",
    "measure:legacy": "tsx scripts/measure-all-lighthouse.ts",
    "generate:charts": "python3 scripts/generate-charts.py",
    "build:all": "for dir in kanban-*/; do (cd \"$dir\" && npm run build); done",
    "setup:all": "for dir in kanban-*/; do (cd \"$dir\" && if grep -q '\"setup\"' package.json 2>/dev/null; then echo \"\\n🔧 Setting up $dir...\" && npm run setup; else echo \"\\n⏭️  Skipping $dir (no setup script)\"; fi); done"
  },
  "keywords": [
    "react",
    "vue",
    "svelte",
    "solid",
    "qwik",
    "angular",
    "nextjs",
    "nuxt",
    "sveltekit",
    "solidstart",
    "htmx",
    "marko",
    "tanstack",
    "analog",
    "framework-comparison",
    "performance"
  ],
  "author": "Loren Stewart",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prettier": "^3.6.2",
    "prettier-plugin-marko": "^3.3.2",
    "tsx": "^4.7.0"
  }
}
